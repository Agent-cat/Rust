fn variable() {
    let x: i32 = 2;
    let y: u32 = 60;
    let z: f32 = 30.16;

    /* There are different sizes like i8 i32 i64 etc
        u --> unsigned it means only positive
        i --> signed positive and degstive (- +)
        z --> f stands for float
    */

    println!("x varshan :{}", x);
    println!("y vishnu:{}", y);
    println!("z mandala :{}", z)
}
